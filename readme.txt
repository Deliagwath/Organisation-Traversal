The program's aim is to take in a file and two arguments, being the beginning
and end point of a path to be returned.

The program is to be run using the command line with the command:
python BT.py [Chart] [Beginning Name] [Ending Name]
Without the square brackets ([])